# PROJECT_BRIEF

## 1. 课程与项目类型

这是 AI4SE 期末项目，选择 B：非 harness 应用类项目。

完整要求 = `docs/assignment/通用要求.md` + `docs/assignment/AI4SE_Final_Project_B_应用类项目.md`。

项目名称：MixMyFit。

当前分支：`docs/brainstorming-spec`。

当前阶段：SPEC 阶段。`SPEC.md` 已完成初稿并按人工反馈修订；`SPEC_PROCESS.md` 已记录 brainstorming 过程；尚未进入 `PLAN.md`。

## 2. 强制流程约束

1. 必须使用 Superpowers 全流程。
2. 在 `SPEC.md` 和 `PLAN.md` 完成并通过冷启动验证前，禁止编写任何业务实现代码。
3. 当前不得进入实现阶段。
4. 当前不得直接编写 `PLAN.md`，除非用户明确确认 SPEC 阶段文档已完成，并要求进入 `writing-plans`。
5. 后续进入实现阶段时必须遵守 TDD：先写失败测试，再写最小实现，再重构。
6. 必须维护 `SPEC.md`、`PLAN.md`、`SPEC_PROCESS.md`、`AGENT_LOG.md`、`README.md`、`REFLECTION.md`。
7. 前端 / UI 开发必须遵循 Open Design，并在 SPEC 和后续实现说明中记录所选设计系统与 skill。
8. 必须有可一键运行测试。
9. 必须有 CI，`.gitlab-ci.yml` 中包含名为 `unit-test` 的 job。
10. 必须考虑凭据安全和分发。
11. 最终需要提供线上可访问 WebUI。

## 3. 项目定位

MixMyFit 是一个面向普通用户的个人衣橱搭配管理 Web 应用。

用户可以上传自己的衣物图片，对衣物进行分类、筛选和标签管理，并在搭配编辑器中组合、预览、保存和复用穿搭方案。

项目不做电商、不做社交平台、不做 AI 穿搭推荐，也不构建 agent。核心价值是让用户不用反复试穿或手工拼图，就能基于自己的真实衣物图片完成搭配规划。

目标用户：

- 拥有一定数量衣物、希望用数字化方式管理衣物并进行穿搭组合的普通用户。

典型使用场景：

- 日常通勤或上学前快速挑选穿搭。
- 穿搭爱好者整理衣柜和风格方案。
- 旅行或出差前规划要带的衣物组合。
- 用户复用以前保存过的搭配方案。

这些是使用场景，不是互斥用户类型。

## 4. MVP 边界

MVP 必须完成以下闭环：

用户注册登录 -> 上传衣物 -> 补全品类等信息 -> 管理标签和筛选衣物 -> 在搭配编辑器中组合衣物 -> 保存搭配方案 -> 查看、编辑、删除和筛选搭配方案。

MVP 必做模块：

- 账号认证与个人资料管理。
- 衣物图片上传与衣物管理。
- 固定品类与自定义品类。
- 自定义颜色、季节、多选标签。
- 批量上传、待完善提醒、批量设置品类/颜色/季节/标签。
- 搭配编辑器：四个固定主槽位 + 自定义品类配饰层。
- 搭配方案保存、查看、编辑内容与信息、删除、筛选。
- 权限隔离、上传文件安全、密码与 Cookie 安全。
- Docker Compose 分发、GitLab CI、测试与线上 WebUI。

明确不进入 MVP：

- AI / LLM / agent。
- 手机号注册、邮箱验证、找回密码。
- 社交、点赞、收藏公开搭配。
- 电商购买链接。
- 图片背景去除。
- 正式多级品类 / 子分类系统。
- 自由画布式完整穿搭拼贴编辑器。

子分类和风格先通过标签表达。AI 推荐、背景去除、分享社区可以作为未来扩展，但不作为当前验收范围。

## 5. 核心功能设计

### 5.1 账号与会话

- 注册使用唯一用户名 + 密码 + 可选昵称。
- 用户注册后系统分配内部 `user_id`。
- 用户名用于登录，`user_id` 用于关联业务数据。
- 支持登录、退出、查看用户名、修改昵称、修改密码。
- 不实现手机号、邮箱验证、找回密码、第三方登录。
- 登录态采用后端设置 Cookie，前端不直接管理 token。
- Cookie 必须设置 HttpOnly。
- 线上 HTTPS 时 Cookie 必须设置 Secure。
- SameSite 根据部署情况设置。
- 登出时后端清除 Cookie。
- 后端接口必须校验当前登录用户。
- 用户只能访问自己的衣物、品类、标签、搭配方案和上传图片。

### 5.2 衣物管理

固定基础品类：

- 上装。
- 下装。
- 鞋子。
- 帽子。

用户可以创建自定义一级品类，例如耳环、项链、手链、包、围巾等。

衣物用于搭配的最低要求：

- 图片 + 品类。

衣物规则：

- 上传图片成功即可创建衣物草稿。
- 缺少品类的衣物标记为“待完善”。
- 待完善衣物可以保存在衣物库中，但不能加入搭配编辑器。
- 名称不是必填项。
- 颜色、季节、标签为可选字段。
- 衣物信息支持修改，包括品类、名称、颜色、季节和标签。

衣物属性：

- 品类：固定品类或用户自定义一级品类。
- 颜色：用户自定义，支持从已有颜色中复用。
- 季节：春、夏、秋、冬，可多选。
- 标签：用户自定义，可多选，用于表达子分类、风格、材质、特点或场景倾向。

批量操作：

- 批量设置品类。
- 批量设置颜色。
- 批量设置季节。
- 批量添加标签。
- 批量移除标签。

待完善衣物必须能通过批量设置品类快速补全。

衣物筛选至少支持：

- 品类。
- 是否待完善。
- 颜色。
- 季节。
- 标签。

### 5.3 搭配编辑器

搭配编辑器采用“结构化主槽位 + 可选自由配饰层”。

固定主槽位：

- 上装槽。
- 下装槽。
- 鞋子槽。
- 帽子槽。

每个主槽位支持：

- 左右箭头切换该品类下的衣物。
- 打开该品类衣物库直接选择。
- 按颜色、季节和标签筛选当前槽位的候选衣物。
- 清空当前槽位。

四个固定主槽位都可以为空。用户不需要选满主槽位。

自定义品类默认不自动变成固定槽位。用户可以从自定义品类中选择物品作为配饰添加到搭配预览中。

配饰层支持：

- 添加配饰。
- 移除配饰。
- 拖动位置。
- 调整层级。
- 小、中、大三个尺寸档位。

MVP 不实现自由比例缩放、旋转、复杂画布裁剪或完整拼贴编辑器。

### 5.4 搭配方案

搭配方案允许部分主槽位为空。保存条件是至少包含一件已选择的衣物或配饰。

保存内容包括：

- 固定主槽位选择。
- 配饰项。
- 配饰位置。
- 配饰尺寸档位。
- 配饰层级。
- 方案名称。
- 备注。
- 场景标签。
- 季节标签。

方案名称可选；为空时系统生成默认标题。

搭配方案必须支持：

- 创建。
- 查看详情。
- 编辑搭配内容。
- 编辑方案名称、备注、场景标签和季节标签。
- 删除。
- 按场景标签筛选。
- 按季节筛选。
- 标签和季节组合筛选。

## 6. 技术栈与架构决策

后端：

- Spring Boot。
- 提供 REST API。
- 负责认证、权限、业务规则、文件上传、数据访问。

前端：

- Vue。
- 前后端分离。
- 负责衣物管理界面、批量操作、搭配编辑器和方案筛选。
- WebUI 设计遵循 Open Design。

数据库：

- MySQL。
- 存储用户、品类、衣物元数据、标签、搭配方案和搭配明细。

文件存储：

- 服务端本地文件系统。
- 数据库只保存文件路径、原始文件名、内容类型、大小等元数据。
- Docker 运行时用 volume 持久化上传目录。

分发：

- Docker Compose。
- 一套命令启动前端、后端和 MySQL。

线上部署：

- 最终需要提供可访问 WebUI。
- 候选平台包括 Render、Railway、Fly.io 或其他支持容器部署的平台。
- 具体平台在部署阶段再次询问用户并确认。

## 7. 安全与凭据边界

MixMyFit MVP 不调用 LLM、agent 或外部 AI API，因此不需要用户配置 LLM API key。

仍需保护：

- 用户密码。
- 会话 Cookie。
- 数据库连接配置。
- 生产环境配置。
- 上传文件访问路径。

安全要求：

- 密码只保存哈希，不保存明文。
- 数据库密码等部署配置不得硬编码进源码。
- 真实生产凭据不得提交到 Git。
- 开发和部署配置通过环境变量或 Docker Compose 配置注入。
- 上传文件必须限制 MIME 类型和大小。
- 服务端生成文件名，不信任用户上传的原始文件名。
- 文件访问接口必须校验当前用户是否拥有该文件。
- 日志不得记录密码、Cookie、数据库密码或真实生产凭据。

## 8. 数据模型要求

`SPEC.md` 中的数据模型已经要求所有核心实体使用可区分 ID 名称，不能只写通用 `id`。

关键实体包括：

- `users.user_id`
- `categories.category_id`
- `clothes.clothing_id`
- `clothing_seasons.clothing_season_id`
- `clothing_tags.clothing_tag_id`
- `clothing_tag_links.clothing_tag_link_id`
- `outfit_tags.outfit_tag_id`
- `outfits.outfit_id`
- `outfit_seasons.outfit_season_id`
- `outfit_tag_links.outfit_tag_link_id`
- `outfit_items.outfit_item_id`

所有业务数据必须通过 `user_id` 做用户隔离。

## 9. 测试与 CI

测试策略为前后端均衡，外加少量 E2E。

后端测试重点：

- 认证与会话。
- 用户隔离。
- 衣物管理。
- 批量设置品类、颜色、季节和标签。
- 文件上传校验。
- 搭配保存、编辑、删除、筛选。

前端测试重点：

- 衣物列表。
- 批量上传反馈。
- 待完善筛选。
- 批量操作。
- 搭配编辑器槽位切换。
- 搭配编辑器候选衣物筛选。
- 配饰添加、拖动、层级和尺寸档位。
- 搭配方案筛选。

E2E 测试重点：

- 注册登录 -> 批量上传衣物 -> 批量补全品类/颜色/季节/标签 -> 创建搭配 -> 保存方案 -> 按标签筛选。
- 用户 A 不能访问用户 B 的衣物或搭配方案。

CI 要求：

- `.gitlab-ci.yml` 必须包含名为 `unit-test` 的 job。
- `unit-test` job 至少运行后端核心测试。
- CI 应能构建或校验前端。

## 10. 当前文档状态

- `SPEC.md`：已完成初稿，并根据人工反馈修订。
- `SPEC_PROCESS.md`：已记录 brainstorming 关键迭代，冷启动验证部分尚未完成。
- `README.md`：已按 SPEC 阶段更新项目简介、目标用户、核心功能、当前状态和安全边界；安装、运行、测试、分发、部署命令仍应标记为尚未实现。
- `PLAN.md`：尚未开始。
- `AGENT_LOG.md`：目前是模板，后续实现阶段需要持续维护。
- `REFLECTION.md`：尚未完成。

## 11. README 更新流程

README 必须最终覆盖作业要求中的项目简介、安装、运行、分发命令、目录结构、安全边界说明，也应让陌生人知道项目用途、怎么安装、怎么运行、怎么测试、怎么配置 key、怎么部署或获取分发产物、有什么限制。

但 README 需要按项目阶段逐步更新，不能在尚未实现时写成已经可运行。

阶段性更新规则：

1. SPEC 完成后：更新项目简介、目标用户、核心功能、当前状态、安全边界和已知限制。
2. PLAN 完成后：更新技术栈、目录规划、测试计划、分发计划，但仍不能写不存在的实际命令。
3. 每个实现 task 完成后：如果改变运行方式、功能说明、目录结构或限制，就同步更新 README。
4. CI / Docker / 部署完成后：补充最终安装命令、运行命令、测试命令、Docker/分发命令、部署说明和线上 URL。
5. 最终提交前：完整检查 README 是否覆盖课程要求的所有章节和真实可执行命令。

当前 README 只应反映 SPEC 阶段已经真实完成的内容：

- 项目有什么用。
- 目标用户是谁。
- MVP 核心功能是什么。
- 当前尚无可运行应用。
- 安装、运行、测试、分发、部署仍待后续阶段补充。
- 不使用 LLM / agent / 外部 AI API，但后续仍需保护用户密码、Cookie、数据库配置和上传文件访问路径。

## 12. 给新 AI 会话的工作提示

如果新会话接手当前项目，应先阅读：

1. `docs/assignment/通用要求.md`
2. `docs/assignment/AI4SE_Final_Project_B_应用类项目.md`
3. `SPEC.md`
4. `SPEC_PROCESS.md`
5. `PROJECT_BRIEF.md`

在用户明确要求前，不要写业务实现代码。

在用户确认 SPEC 阶段文档完成前，不要进入 `PLAN.md`。

如果用户要求进入计划阶段，必须先使用 Superpowers `writing-plans` 技能，把 `SPEC.md` 拆成细粒度任务，并为每个任务写清目标、涉及文件、预期实现要点、失败测试和验证步骤。
