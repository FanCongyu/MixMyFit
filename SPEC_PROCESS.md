# SPEC_PROCESS.md

## 1. Brainstorming 总览

- 主开发智能体：OpenAI Codex，使用 Superpowers `brainstorming` 技能。
- 当前分支：`docs/brainstorming-spec`。
- 开始日期：2026-07-10。
- 项目类型：AI4SE 期末项目 B，非 harness 应用类项目。
- 项目名称：MixMyFit。
- 当前阶段：SPEC 阶段。`SPEC.md` 已形成初稿并根据人工反馈修订；尚未进入 `PLAN.md`。

项目初始想法：

MixMyFit 最初被描述为一个衣物搭配与套装保存平台。目标用户是普通用户，核心功能初步包括注册登录、服装图片上传与分类、搭配组合、搭配方案保存与管理。项目一开始没有明确技术栈、衣物管理粒度、搭配编辑器交互、凭据边界、分发方式和测试策略。

Brainstorming 后的当前结论：

- MixMyFit 定位为个人衣橱搭配管理 Web 应用。
- 项目不使用 LLM、外部 AI API 或 agent。
- 后端采用 Spring Boot REST API，前端采用 Vue，数据库采用 MySQL。
- WebUI 开发需要遵循 Open Design，并在 SPEC 中说明。
- 图片文件存储在服务端本地文件系统，数据库保存元数据和路径。
- 分发方式采用 Docker Compose，最终需要线上可访问 WebUI。
- 在 `SPEC.md` 和后续 `PLAN.md` 完成并通过冷启动验证前，不进入实现阶段。

## 2. 关键迭代记录

### 迭代 1：项目方向与目标用户澄清

Codex 提出的问题：

- 项目应定位为个人衣橱搭配管理、智能穿搭建议，还是衣物素材整理工具？
- 目标用户是否应拆分为日常通勤/上学用户、穿搭爱好者、旅行/出差规划用户？

我的回答：

- 选择个人衣橱搭配管理 Web 应用。
- 不同的通勤、穿搭兴趣、旅行规划等不是互斥目标用户，而是衣物搭配用户的不同使用场景。

Codex 给出的建议：

- 建议项目采用完成度优先的个人衣橱搭配管理方向。
- 建议不要在 MVP 中承诺 AI 推荐。
- 最初建议按场景拆分目标用户。

我采纳的内容：

- 采纳个人衣橱搭配管理的方向。
- 采纳不使用 AI / LLM / agent 的边界。
- 采纳“目标用户是希望数字化管理衣物并进行搭配的普通用户”的宽泛定义。

我拒绝或修改的内容：

- 修改了 Codex 将通勤、穿搭爱好者、旅行规划用户作为互斥目标用户的表述。

修改原因：

- 这些人群本质上都是“想要对衣物进行搭配的用户”，差异在使用场景，不应过早切分成互斥目标用户。

### 迭代 2：衣物管理模型与批量操作

Codex 提出的问题：

- 衣物管理是否只需要固定品类、颜色和季节？
- 子分类是否应进入 MVP？
- 标签是否需要自定义？
- 衣物可用于搭配的最低完整性要求是什么？

我的回答：

- 固定基础品类需要包括上装、下装、鞋子、帽子。
- 用户必须能创建自定义一级品类，例如耳环、项链、手链等。
- 颜色需要支持用户自定义。
- 季节需要支持。
- 子分类暂时不做正式系统。
- 标签需要支持自定义和多选。
- 衣物用于搭配的最低要求是图片 + 品类，名称不应必填。
- 需要批量操作，包括批量设置品类、颜色、季节，以及批量添加/移除标签。
- 待完善衣物需要支持批量设置品类。

Codex 给出的建议：

- 固定基础品类 + 用户自定义一级品类 + 自定义颜色 + 多选季节 + 可选自定义标签。
- 不做正式子分类系统，短袖、衬衫、毛衣、外套、复古等先通过标签表达。
- 批量上传后生成待完善衣物，缺少品类的衣物不能进入搭配编辑器。
- 最初建议衣物至少需要图片 + 名称 + 品类才能进入搭配。

我采纳的内容：

- 采纳固定基础品类与自定义一级品类并存的模型。
- 采纳用标签承载子分类、风格、材质等个性化描述。
- 采纳待完善衣物状态。
- 采纳批量上传后集中补全信息的录入方式。

我拒绝或修改的内容：

- 拒绝“名称必填”的建议。
- 扩展批量操作，不只批量标签，还包括批量设置品类、颜色和季节。

修改原因：

- MixMyFit 是视觉搭配工具，用户主要通过图片识别衣物，不是库存管理系统。不是所有用户都愿意给每件衣物命名。
- 用户一次上传多件衣物时，如果不能批量设置品类、颜色、季节和标签，录入成本会过高。

### 迭代 3：搭配编辑器与方案保存

Codex 提出的问题：

- 搭配编辑器应采用固定槽位、自由画布，还是混合方式？
- 用户没有选择帽子、鞋子等槽位时是否允许保存？
- 搭配方案保存时需要哪些信息？

我的回答：

- 选择混合方式。
- 希望页面横向分为几个部分，每个部分通过左右箭头更换衣物图片，也能打开该品类衣物库直接挑选。
- 饰品类衣物不适合固定槽位，更适合放到可移动、可叠放的区域。
- 不应强制用户选满固定主槽位。
- 搭配方案需要保存名称、备注、场景标签、季节标签，并且能按标签筛选。
- 搭配编辑器中挑选衣物时也需要能按颜色、季节、标签筛选候选衣物。

Codex 给出的建议：

- 采用“结构化主槽位 + 可选自由配饰层”。
- 固定主槽位为上装、下装、鞋子、帽子。
- 自定义品类默认进入配饰层，不自动成为固定槽位。
- 配饰支持添加、移除、拖动、层级调整，小/中/大尺寸档位。
- 搭配方案允许部分主槽位为空，至少包含一件衣物或配饰即可保存。

我采纳的内容：

- 采纳四个固定主槽位 + 自定义品类配饰层。
- 采纳空槽位允许保存。
- 采纳配饰层的最小可用交互。
- 采纳搭配方案名称、备注、场景标签、季节标签。

我拒绝或修改的内容：

- 限制自由画布能力，不做完整自由缩放、旋转、裁剪或复杂拼贴。
- 补充了搭配编辑器内候选衣物筛选能力。

修改原因：

- 完整自由画布会显著增加前端复杂度、移动端适配和测试成本。
- 当衣物数量变多时，单纯左右切换效率不够，必须在编辑器内配合颜色、季节、标签筛选。

### 迭代 4：账号、安全、技术栈与分发

Codex 提出的问题：

- 账号注册使用手机号、邮箱还是用户名？
- 是否涉及 LLM、agent 或外部 AI API？
- 技术栈采用 Spring Boot、FastAPI 还是 Node.js？
- 图片如何存储？
- 分发采用 Docker Compose、单独 Docker 镜像还是手动部署？
- 认证采用 Session、JWT 还是 Cookie 登录态？

我的回答：

- 采用唯一用户名 + 密码 + 可选昵称。
- 用户注册后需要系统内部 ID。
- 不使用 AI、LLM、agent 或外部 AI API。
- 采用 Spring Boot REST API + Vue + MySQL。
- 图片存本地文件系统，数据库保存路径和元数据。
- 分发采用 Docker Compose。
- 线上部署暂定云平台，后续部署阶段再次确认具体平台。
- 登录态采用后端设置 Cookie，要求 HttpOnly；线上 HTTPS 时设置 Secure；SameSite 根据部署情况设置；登出时后端清除 Cookie；后端接口必须校验当前登录用户；用户只能访问自己的衣服和搭配数据。

Codex 给出的建议：

- 建议唯一用户名 + 密码，避免手机号和邮箱引入额外外部服务和凭据。
- 建议不引入 AI，凭据安全重点放在数据库配置、用户密码、Cookie、上传文件和日志。
- 建议 Spring Boot REST API + Vue + MySQL + Docker Compose。
- 建议本地文件系统 + Docker volume 持久化上传目录。
- 建议用后端 Cookie 登录态，前端不保存 token。

我采纳的内容：

- 采纳唯一用户名 + 密码 + 可选昵称。
- 采纳内部 `user_id` 作为不可变业务关联字段。
- 采纳不使用 AI / LLM / agent。
- 采纳 Spring Boot REST API + Vue + MySQL。
- 采纳 Docker Compose 分发。
- 采纳 Cookie 登录态和用户隔离安全要求。

我拒绝或修改的内容：

- 暂不绑定具体线上部署平台。
- 要求前端开发明确使用 Open Design，并在 SPEC 中说明所选设计系统与 skill。

修改原因：

- 线上部署平台选择会受到免费额度、MySQL 支持、volume 支持和 HTTPS 配置影响，应在部署阶段重新确认。
- 课程通用要求明确推荐前端/UI 项目使用 Open Design，需要在 SPEC 和后续实现中体现。

### 迭代 5：SPEC 初稿审阅与修订

Codex 提出的问题或初稿内容：

- 将已确认内容整理成 `SPEC.md` 的 9 个部分，包括项目定位、功能模块、衣物管理、搭配编辑器、账号安全、架构数据模型、非功能需求、分发测试验收、风险未决问题。
- 数据模型初稿中部分实体主键使用了通用 `id`。
- 搭配编辑器初稿没有明确候选衣物筛选。
- 批量操作初稿主要强调标签操作。

我的反馈：

- 搭配编辑器应该配合筛选衣物功能使用，挑选上装等槽位时能按颜色、季节、标签筛选候选衣物。
- 待完善衣物需要能批量选择品类。
- 批量操作还要支持批量添加颜色、季节。
- 衣物信息和搭配方案信息都支持修改。
- 标签支持多选。
- 数据模型中每个实体的 ID 要用可区分名称，例如 `user_id`，其他实体也对应修改。

Codex 给出的修改：

- 在 `SPEC.md` 中补充编辑器候选衣物筛选。
- 补充批量设置品类、颜色、季节和批量添加/移除标签。
- 明确待完善衣物可以批量设置品类。
- 明确衣物信息、搭配方案内容和信息均支持编辑。
- 明确标签支持多选。
- 将数据模型主键改为 `user_id`、`category_id`、`clothing_id`、`outfit_id`、`outfit_item_id` 等可区分字段。

我采纳的内容：

- 采纳上述修订，并确认 `SPEC.md` 当前暂不需要继续修改。

我拒绝或修改的内容：

- 暂未进入 `PLAN.md`，因为 SPEC 阶段还需要补齐 `SPEC_PROCESS.md` 和 `PROJECT_BRIEF.md`。

修改原因：

- 通用要求规定 `SPEC_PROCESS.md` 是规约与计划生成过程的重要交付物，不能在过程文档未补齐时提前进入计划阶段。

## 3. 当前 SPEC 结论摘要

`SPEC.md` 当前定义的项目边界如下：

- 项目是个人衣橱搭配管理 Web 应用。
- 不使用 AI、LLM、agent、手机号、邮箱验证、找回密码、社交、电商、图片背景去除或正式子分类系统。
- MVP 包含账号认证、衣物管理、搭配编辑器、搭配方案管理、文件与安全、分发运维等模块。
- 衣物管理支持固定品类、自定义一级品类、自定义颜色、季节、多选标签、批量上传、待完善提醒、批量设置品类/颜色/季节/标签。
- 衣物用于搭配的最低要求是图片 + 品类；名称不是必填。
- 搭配编辑器包含上装、下装、鞋子、帽子四个固定主槽位，并支持按颜色、季节、标签筛选候选衣物。
- 自定义品类默认进入配饰层，支持添加、移除、拖动、层级调整、小/中/大尺寸档位。
- 搭配方案支持保存、查看、编辑内容与信息、删除，并按场景标签和季节筛选。
- 技术栈为 Spring Boot REST API + Vue + MySQL。
- 图片存储在服务端本地文件系统，Docker volume 持久化。
- 分发采用 Docker Compose；最终需要线上 WebUI，具体云平台后续确认。
- 测试策略为后端、前端和少量 E2E 均衡覆盖；GitLab CI 必须包含 `unit-test` job。

## 4. 冷启动验证记录

当前状态：未进行。

原因：

- 冷启动验证需要仅提供 `SPEC.md` + `PLAN.md` 给第二个、不同类型的智能体。
- 当前还没有进入 `PLAN.md` 阶段，因此无法进行符合要求的冷启动验证。

后续要求：

- 完成并确认 `PLAN.md` 后，启动一个与主开发智能体不同的新智能体。
- 不提供本会话历史、`SPEC_PROCESS.md` 或口头解释。
- 只提供 `SPEC.md` 和 `PLAN.md`。
- 要求它选择 1 到 2 个 task 自主尝试，并明确遇到不确定之处应暂停提问，而不是猜测继续。
- 将它的疑问、误解、暴露出的 SPEC/PLAN 缺陷、修订前后 diff 摘要补充回本文件。

## 5. 对 brainstorming 技能的阶段性反思

做得好的地方：

- Brainstorming 过程强制在实现前澄清范围，避免过早开始写代码。
- 多轮追问帮助项目从“衣物搭配平台”收敛成有明确边界的 Web 应用。
- 对衣物管理、搭配编辑器、账号安全、分发和测试逐步拆分，降低了后续 PLAN 阶段遗漏关键工程要求的风险。
- 通过人工 review，发现并修正了目标用户拆分、衣物名称是否必填、批量操作不足、编辑器筛选缺失、数据模型 ID 命名不清等问题。

不足之处：

- 初始问题有时把“目标用户”和“使用场景”混在一起，需要人工纠正。
- 初稿中部分规则偏向通用 CRUD，例如建议名称必填，但没有充分贴合视觉搭配工具的使用习惯。
- 初版 SPEC 对批量操作和编辑器筛选写得不够完整，需要人工补充真实使用场景。
- 数据模型初稿中通用 `id` 字段不利于后续 subagent 冷启动实现，需要人工要求改成可区分字段。

我如何干预和纠偏：

- 明确指出通勤、旅行、穿搭兴趣是使用场景，不是互斥目标用户。
- 明确衣物名称不应必填，衣物最低完整性为图片 + 品类。
- 要求补充批量设置品类、颜色、季节和多选标签，降低大量录入时的操作成本。
- 要求搭配编辑器中的候选衣物也支持颜色、季节、标签筛选。
- 要求数据模型主键使用 `user_id`、`clothing_id`、`outfit_id` 等可区分名称，避免后续实现歧义。

## 6. 下一步

在 `SPEC_PROCESS.md` 和 `PROJECT_BRIEF.md` 更新完成后，应先由用户 review 这两份文件。

确认无误后，下一步才是使用 Superpowers `writing-plans` 技能创建 `PLAN.md`。`PLAN.md` 必须把 `SPEC.md` 拆成细粒度 task，每个 task 包含目标、涉及文件、预期实现要点、失败测试和验证步骤，并显式标出依赖与可并行部分。
