# MixMyFit SPEC

## 1. 项目定位、目标用户与 MVP 边界

### 1.1 项目定位

MixMyFit 是一个面向普通用户的个人衣橱搭配管理 Web 应用。用户可以上传自己的衣物图片，对衣物进行分类、筛选和标签管理，并在搭配编辑器中组合、预览、保存和复用穿搭方案。

本项目不做电商、不做社交平台、不做 AI 穿搭推荐，也不构建 agent。核心价值是让用户不用反复试穿或手工拼图，就能基于自己的真实衣物图片完成搭配规划。

### 1.2 目标用户

目标用户是拥有一定数量衣物、希望用数字化方式管理衣物并进行穿搭组合的普通用户。

典型使用场景包括：

- 日常通勤或上学前快速挑选穿搭。
- 穿搭爱好者整理自己的衣柜和风格方案。
- 旅行或出差前规划要带的衣物组合。
- 用户想复用以前保存过的搭配方案。

这些是使用场景，不是互斥的用户类型。

### 1.3 MVP 必做范围

MVP 必须完成以下闭环：

用户注册登录 -> 上传衣物 -> 补全品类等信息 -> 管理标签和筛选衣物 -> 在搭配编辑器中组合衣物 -> 保存搭配方案 -> 查看、编辑、删除和筛选搭配方案。

MVP 必做模块：

- 账号认证与个人资料管理。
- 衣物图片上传与衣物管理。
- 固定品类与自定义品类。
- 自定义颜色、季节、多标签。
- 批量上传、待完善提醒、批量品类/颜色/季节/标签操作。
- 搭配编辑器：四个固定主槽位 + 自定义品类配饰层。
- 搭配方案保存、查看、编辑内容与信息、删除、筛选。
- 权限隔离、上传文件安全、密码与 Cookie 安全。
- Docker Compose 分发、GitLab CI、测试与线上 WebUI。

### 1.4 明确不进入 MVP 的内容

- AI / LLM / agent。
- 手机号注册、邮箱验证、找回密码。
- 社交、点赞、收藏公开搭配。
- 电商购买链接。
- 图片背景去除。
- 真正的多级品类 / 子分类系统。
- 自由画布式完整穿搭拼贴编辑器。

子分类和风格先通过标签表达。AI 推荐、背景去除、分享社区可以作为未来扩展，不作为当前验收范围。

## 2. 功能模块与用户故事

### 2.1 功能模块

系统按职责拆分为以下模块：

1. 账号与会话模块：注册、登录、退出、个人资料、修改密码、登录态 Cookie 管理。
2. 衣物管理模块：图片上传、衣物信息维护、固定品类、自定义品类、颜色、季节、标签、待完善状态、批量品类/颜色/季节/标签操作。
3. 搭配编辑器模块：固定主槽位切换、按品类和筛选条件选择衣物、自定义品类配饰层、拖动、层级调整、尺寸档位。
4. 搭配方案模块：保存、查看、编辑信息、删除、按标签和季节筛选。
5. 文件与安全模块：上传文件校验、本地文件存储、文件访问权限控制。
6. 分发与运维模块：Docker Compose、本地 volume、GitLab CI、部署说明。

### 2.2 后端 API 最小合约

后端 REST API 必须在实现阶段至少提供以下合约。字段名、HTTP 方法、状态码和错误格式应作为前后端与自动化测试的共同基线，除非后续文档显式修订。

统一约定：

- 所有 JSON 请求和响应使用 `application/json`。
- 创建资源成功返回 `201 Created`。
- 登录成功返回 `200 OK`。
- 登出成功返回 `204 No Content`。
- 未登录访问需要认证的接口返回 `401 Unauthorized`。
- 请求字段校验失败、空搭配保存、非法文件类型或文件过大返回 `400 Bad Request`。
- 访问其他用户资源时返回 `403 Forbidden` 或 `404 Not Found`，不得泄露资源是否存在。
- 错误响应统一为 `{ "code": "ERROR_CODE", "message": "Human readable message" }`。

认证 API：

| 方法 | 路径 | 请求 | 成功响应 |
|---|---|---|---|
| POST | `/api/auth/register` | `{ "username": string, "password": string, "confirmPassword": string, "nickname"?: string }` | `201`, `{ "userId": number, "username": string, "nickname"?: string }` |
| POST | `/api/auth/login` | `{ "username": string, "password": string }` | `200`, `{ "userId": number, "username": string, "nickname"?: string }`，并设置登录 Cookie |
| POST | `/api/auth/logout` | 无请求体 | `204`，并清除登录 Cookie |
| GET | `/api/profile` | 无请求体 | `200`, `{ "userId": number, "username": string, "nickname"?: string }` |
| PATCH | `/api/profile` | `{ "nickname": string }` | `200`, `{ "userId": number, "username": string, "nickname"?: string }` |
| POST | `/api/profile/password` | `{ "oldPassword": string, "newPassword": string }` | `204` |

衣物与文件 API：

| 方法 | 路径 | 请求 | 成功响应 |
|---|---|---|---|
| POST | `/api/clothes` | `multipart/form-data`，文件字段名固定为 `file`，可选字段包括 `categoryId`、`name`、`color`、`seasons`、`tagIds` | `201`, `{ "clothingId": number, "status": "draft or ready", "imageUrl": string, "originalFilename": string, "contentType": string, "fileSize": number }` |
| GET | `/api/clothes/{clothingId}` | 无请求体 | `200`, 衣物详情 |
| GET | `/api/clothes` | query：`page`、`size`、`categoryId`、`status`、`color`、`season`、`tagIds` | `200`, `{ "items": Clothing[], "page": number, "size": number, "total": number }` |
| PATCH | `/api/clothes/{clothingId}` | `{ "categoryId"?: number, "name"?: string, "color"?: string, "seasons"?: string[], "tagIds"?: number[] }` | `200`, 衣物详情 |
| DELETE | `/api/clothes/{clothingId}` | 无请求体 | `204` |
| POST | `/api/clothes/batch` | `{ "clothingIds": number[], "categoryId"?: number, "color"?: string, "seasons"?: string[], "addTagIds"?: number[], "removeTagIds"?: number[] }` | `200`, `{ "updated": number }` |
| GET | `/api/clothes/{clothingId}/image` | 无请求体 | `200`, 图片二进制，必须校验归属 |

搭配 API：

| 方法 | 路径 | 请求 | 成功响应 |
|---|---|---|---|
| POST | `/api/outfits` | `{ "title"?: string, "note"?: string, "seasons"?: string[], "tagIds"?: number[], "items": OutfitItem[] }` | `201`, `{ "outfitId": number, "title": string }` |
| GET | `/api/outfits/{outfitId}` | 无请求体 | `200`, 搭配详情 |
| GET | `/api/outfits` | query：`page`、`size`、`season`、`tagIds` | `200`, `{ "items": OutfitSummary[], "page": number, "size": number, "total": number }` |
| PATCH | `/api/outfits/{outfitId}` | 可更新标题、备注、季节、标签和 items | `200`, 搭配详情 |
| DELETE | `/api/outfits/{outfitId}` | 无请求体 | `204` |

列表接口默认 `page = 0`、`size = 20`，默认按 `created_at desc` 排序。

### 2.3 用户故事

US-1：作为新用户，我希望能用唯一用户名和密码注册账号，这样我的衣物和搭配方案不会和其他用户混在一起。

验收标准：

- 用户名必须唯一。
- 密码必须加密哈希存储。
- 注册成功后系统创建内部用户 ID。
- 注册失败时返回明确但不泄露敏感细节的错误信息。

US-2：作为已登录用户，我希望能批量上传衣物图片，这样我可以快速把已有衣物录入系统。

验收标准：

- 用户可以一次选择多张图片上传。
- 上传成功后每张图片生成一条衣物记录。
- 未设置品类的衣物显示为“待完善”。
- 系统提示当前有多少件衣物待完善。

US-3：作为用户，我希望能给衣物设置品类、颜色、季节和标签，这样我可以按自己的方式查找衣物。

验收标准：

- 基础固定品类包括上装、下装、鞋子、帽子。
- 用户可以创建自定义一级品类。
- 颜色由用户自定义输入或从已有颜色中复用。
- 季节支持春、夏、秋、冬多选。
- 标签为用户自定义、可选，且支持多选。
- 已保存的衣物信息支持修改，包括品类、颜色、季节、标签和名称。

US-4：作为用户，我希望能批量给多件衣物设置品类、颜色、季节和标签，这样我不需要逐件重复编辑。

验收标准：

- 衣物列表支持多选。
- 用户可以对选中的衣物批量添加一个或多个标签。
- 用户可以对选中的衣物批量移除一个或多个标签。
- 用户可以对选中的衣物批量设置品类，用于处理待完善衣物。
- 用户可以对选中的衣物批量设置颜色。
- 用户可以对选中的衣物批量设置季节。
- 批量操作只能影响当前登录用户自己的衣物。

US-5：作为用户，我希望能在搭配编辑器中通过左右箭头快速切换上装、下装、鞋子和帽子，这样我可以边挑选边预览搭配效果。

验收标准：

- 编辑器包含上装、下装、鞋子、帽子四个固定主槽位。
- 每个槽位可以通过左右箭头切换对应品类下的衣物。
- 每个槽位可以打开对应品类衣物库直接选择。
- 每个槽位选择衣物时，可以按颜色、季节和标签筛选候选衣物。
- 每个槽位可以清空。
- 空槽位不阻止保存，只显示为空状态。

US-6：作为用户，我希望能把项链、耳环、包等自定义品类作为配饰添加到搭配预览中，这样搭配方案可以表达更多细节。

验收标准：

- 用户可以从自定义品类中选择 0 到多件配饰。
- 配饰可以添加、移除、拖动。
- 配饰支持层级调整。
- 配饰尺寸支持小、中、大三个档位。
- 保存时记录配饰位置、尺寸和层级。

US-7：作为用户，我希望能保存搭配方案并添加名称、备注、场景标签和季节标签，这样我以后可以按需要复用。

验收标准：

- 至少选择一件衣物或配饰才能保存搭配方案。
- 方案名称可选；为空时系统生成默认标题。
- 备注可选。
- 场景标签支持用户自定义。
- 季节标签支持多选。
- 保存后可以在方案列表中查看。
- 已保存的搭配方案支持修改内容、名称、备注、场景标签和季节标签。

US-8：作为用户，我希望能按标签和季节筛选搭配方案，这样我可以快速找到适合某个场景或季节的穿搭。

验收标准：

- 搭配方案列表支持按场景标签筛选。
- 搭配方案列表支持按季节筛选。
- 支持组合筛选。
- 筛选结果只包含当前登录用户自己的方案。

US-9：作为用户，我希望能修改昵称和密码，这样我可以维护自己的账号信息。

验收标准：

- 用户可以查看自己的用户名。
- 用户可以修改昵称。
- 用户可以在验证旧密码后修改密码。
- 修改密码后旧密码不能继续登录。

## 3. 衣物管理详细规则

### 3.1 衣物完整性规则

衣物用于搭配的最低要求是：图片 + 品类。

- 上传图片成功即可创建衣物草稿。
- 缺少品类的衣物标记为“待完善”。
- 待完善衣物可以保存在衣物库中，但不能加入搭配编辑器。
- 名称不是必填项；用户主要通过图片识别衣物。
- 名称为空时，界面可显示“未命名衣物”或仅显示图片。
- 颜色、季节、标签均为可选字段，但可以用于筛选。
- 衣物信息支持后续修改，包括品类、名称、颜色、季节和标签。

### 3.2 品类规则

系统内置四个固定基础品类：

- 上装。
- 下装。
- 鞋子。
- 帽子。

用户可以创建自定义一级品类，例如耳环、项链、手链、包、围巾等。

MVP 不实现正式子分类系统。短袖、衬衫、毛衣、外套、珍珠、复古等更细粒度描述通过标签表达。这样可以避免品类体系过早复杂化，同时保留用户个性化管理能力。

### 3.3 颜色、季节和标签

颜色：

- 用户自定义输入。
- 系统可以提供已使用颜色的复用选择。
- 颜色不是必填项。

季节：

- 固定为春、夏、秋、冬。
- 支持多选。
- 季节不是必填项。

标签：

- 用户自定义。
- 支持多选。
- 标签可以表达子分类、风格、材质、特点或场景倾向。
- 示例：短袖、衬衫、毛衣、通勤、休闲、复古、牛仔、针织、防晒。
- 标签不是必填项。
- 衣物标签与搭配方案标签分开建模，避免筛选语义混乱。

### 3.4 批量操作

衣物列表必须支持多选，至少提供以下批量操作：

- 批量设置品类。
- 批量设置颜色。
- 批量设置季节。
- 批量添加标签。
- 批量移除标签。

批量设置品类必须能用于处理待完善衣物。用户可以筛选出缺少品类的衣物，一次选择多件衣物并批量设置品类，使这些衣物从“待完善”变为可用于搭配。

批量操作必须校验当前登录用户，只能作用于当前用户自己的衣物。

### 3.5 衣物筛选

衣物库至少支持以下筛选条件：

- 品类。
- 是否待完善。
- 颜色。
- 季节。
- 标签。

## 4. 搭配编辑器与搭配方案规则

### 4.1 搭配编辑器结构

搭配编辑器采用“结构化主槽位 + 可选自由配饰层”的方式。

固定主槽位：

- 上装槽。
- 下装槽。
- 鞋子槽。
- 帽子槽。

每个主槽位支持：

- 左右箭头切换该品类下的衣物。
- 打开该品类衣物库直接选择。
- 按颜色、季节和标签筛选当前槽位的候选衣物。
- 清空当前槽位。

四个固定主槽位都可以为空。用户不需要选满主槽位。

筛选条件只影响当前槽位或当前选择面板中出现的候选衣物，不会修改衣物本身的信息。标签筛选必须支持多选。

### 4.2 配饰层

自定义品类默认不自动变成固定槽位。用户可以从自定义品类中选择物品作为配饰添加到搭配预览中。

配饰层支持：

- 添加配饰。
- 移除配饰。
- 拖动位置。
- 调整层级。
- 使用小、中、大三个尺寸档位。

MVP 不实现自由比例缩放、旋转、复杂画布裁剪或完整拼贴编辑器。

### 4.3 保存规则

搭配方案允许部分主槽位为空。保存条件是至少包含一件已选择的衣物或配饰。

保存内容包括：

- 固定主槽位选择。
- 配饰项。
- 配饰位置。
- 配饰尺寸档位。
- 配饰层级。
- 方案名称。
- 备注。
- 场景标签。
- 季节标签。

方案名称可选。为空时，系统生成默认标题，例如“未命名搭配 2026-07-10 14:30”。

### 4.4 搭配方案管理

搭配方案模块必须支持：

- 创建。
- 查看详情。
- 编辑搭配内容。
- 编辑方案名称、备注、场景标签和季节标签。
- 删除。
- 按场景标签筛选。
- 按季节筛选。
- 标签和季节组合筛选。

所有操作必须限制在当前登录用户自己的数据范围内。

## 5. 账号、安全、权限与凭据边界

### 5.1 账号体系

MVP 采用唯一用户名 + 密码 + 可选昵称的账号体系。

注册字段：

- 用户名。
- 密码。
- 确认密码。
- 昵称，可选。

登录字段：

- 用户名。
- 密码。

个人资料功能：

- 查看用户名。
- 修改昵称。
- 修改密码。

不实现：

- 手机号注册。
- 邮箱验证。
- 找回密码。
- 第三方登录。

用户注册成功后，系统自动分配内部用户 ID。用户名用于登录，内部用户 ID 用于关联衣物、标签、自定义品类、搭配方案等业务数据。

### 5.2 会话与 Cookie

认证机制采用后端设置登录态 Cookie，前端不直接管理 token。

要求：

- Cookie 名称固定为 `MMF_SESSION`。
- Cookie 必须设置 HttpOnly。
- 线上 HTTPS 部署时 Cookie 必须设置 Secure。
- 本地同源开发和 Docker Compose 默认使用 `SameSite=Lax`；若线上前后端跨站部署，必须在部署阶段重新确认 `SameSite=None; Secure` 是否必要。
- Cookie 过期时间默认为 7 天。若后续需要“记住我”，另做扩展，不进入 MVP。
- 登出时由后端清除 Cookie。
- 登出响应必须通过同名 `MMF_SESSION` Cookie 设置 `Max-Age=0` 或等效过期时间完成清除。
- 需要登录的接口必须校验当前用户。

### 5.3 权限隔离

后端必须在所有业务接口中校验当前登录用户，不能只依赖前端隐藏按钮做权限控制。

用户只能访问、修改、删除自己的：

- 衣物。
- 自定义品类。
- 衣物标签。
- 搭配方案标签。
- 搭配方案。
- 搭配方案明细。
- 上传图片文件。

测试必须覆盖用户隔离：用户 A 不能访问、修改或删除用户 B 的数据。

### 5.4 凭据边界

MixMyFit MVP 不调用 LLM、agent 或外部 AI API，因此不需要用户配置 LLM API key。

仍需保护以下敏感信息：

- 用户密码。
- 会话 Cookie。
- 数据库连接配置。
- 生产环境配置。
- 上传文件访问路径。

数据库密码等部署配置不得硬编码进源码，不得提交真实生产凭据到 Git。开发和部署配置应通过环境变量或 Docker Compose 配置注入，并在 README 中说明示例配置与真实凭据的区别。

## 6. 架构、数据模型与技术选型

### 6.1 技术选型

后端：

- Spring Boot。
- Java 17。
- Maven。
- 后端根包名固定为 `com.fan.mixmyfit`。
- 提供 REST API。
- 负责认证、权限、业务规则、文件上传、数据访问。

前端：

- Vue。
- 前后端分离。
- 负责衣物管理界面、批量操作、搭配编辑器和方案筛选。
- WebUI 设计遵循 Open Design，并在实现阶段说明所选设计系统与 skill。

数据库：

- MySQL。
- 数据库迁移工具固定使用 Flyway，迁移文件放在 `backend/src/main/resources/db/migration/`。
- 存储用户、品类、衣物元数据、标签、搭配方案和搭配明细。

文件存储：

- 服务端本地文件系统。
- 数据库只保存文件路径、原始文件名、内容类型、大小等元数据。
- Docker 运行时用 volume 持久化上传目录。
- 上传根目录通过环境变量 `UPLOAD_DIR` 配置；本地开发默认值为 `backend/uploads`，测试环境必须可覆盖为临时目录。
- 单张图片大小上限固定为 5 MB。
- 仅允许 `image/jpeg`、`image/png`、`image/webp`。
- MVP 不生成服务端缩略图；前端必须使用受控展示尺寸，避免直接用原始尺寸撑开布局。

分发：

- Docker Compose。
- 一套命令启动前端、后端和 MySQL。

### 6.2 系统组件

主要组件：

- Vue Web 前端。
- Spring Boot API 服务。
- MySQL 数据库。
- 上传文件存储目录。
- Docker Compose 运行环境。

数据流：

1. 用户通过 Vue 前端注册或登录。
2. Spring Boot 校验账号密码，设置 HttpOnly Cookie。
3. 用户上传图片，Spring Boot 校验文件并写入上传目录。
4. Spring Boot 将衣物元数据写入 MySQL。
5. 用户在前端编辑衣物品类、颜色、季节和标签。
6. 用户在搭配编辑器中选择衣物和配饰。
7. Spring Boot 保存搭配方案及其明细到 MySQL。
8. 前端按标签、季节等条件请求并展示筛选结果。

### 6.3 数据模型

核心实体：

users：

- user_id：内部用户 ID，主键。
- username：唯一登录名。
- password_hash：密码哈希。
- nickname：可选昵称。
- created_at。
- updated_at。

categories：

- category_id。
- user_id：为空表示系统固定品类；不为空表示用户自定义品类。
- name。
- type：fixed 或 custom。
- created_at。
- updated_at。

clothes：

- clothing_id。
- user_id。
- category_id，可为空；为空表示待完善。
- name，可为空。
- color，可为空。
- image_path。
- original_filename。
- content_type。
- file_size。
- status：draft 或 ready。
- created_at。
- updated_at。

clothing_seasons：

- clothing_season_id。
- clothing_id。
- season：spring、summer、autumn、winter。

clothing_tags：

- clothing_tag_id。
- user_id。
- name。
- created_at。
- updated_at。

clothing_tag_links：

- clothing_tag_link_id。
- clothing_id。
- clothing_tag_id。

outfit_tags：

- outfit_tag_id。
- user_id。
- name。
- created_at。
- updated_at。

outfits：

- outfit_id。
- user_id。
- title，可为空或系统生成。
- note，可为空。
- created_at。
- updated_at。

outfit_seasons：

- outfit_season_id。
- outfit_id。
- season：spring、summer、autumn、winter。

outfit_tag_links：

- outfit_tag_link_id。
- outfit_id。
- outfit_tag_id。

outfit_items：

- outfit_item_id。
- outfit_id。
- clothing_id。
- role：main_slot 或 accessory_overlay。
- slot：top、bottom、shoes、hat；仅 main_slot 使用。
- position_x：配饰使用。
- position_y：配饰使用。
- size：small、medium、large；配饰使用。
- z_index：配饰使用。

主要约束：

- username 全局唯一。
- 同一用户下自定义品类名称不重复。
- 同一用户下衣物标签名称不重复。
- 同一用户下搭配方案标签名称不重复。
- 衣物、标签、搭配方案必须通过 user_id 做用户隔离。
- outfit_items 引用的 clothing 必须属于同一用户。

## 7. 非功能性需求

### 7.1 性能

- 普通列表查询应支持分页或懒加载，避免一次加载大量衣物图片元数据。
- 衣物图片展示应使用缩略图或受控尺寸，避免前端一次加载原始大图造成卡顿。
- MVP 不生成服务端缩略图，因此前端必须通过固定容器、`object-fit` 和懒加载控制图片展示尺寸。
- 批量上传应给出明确进度或结果反馈。
- 常见操作如登录、衣物列表加载、搭配方案列表加载，在普通课程演示数据规模下应保持可交互。

### 7.2 安全与凭据威胁模型

威胁：真实数据库密码被提交到仓库。

对策：

- 仓库只提交示例配置。
- 真实配置通过环境变量或部署平台配置注入。
- README 明确说明不要提交真实凭据。

威胁：用户密码泄露。

对策：

- 密码只保存哈希，不保存明文。
- 修改密码需要验证旧密码。
- 登录失败信息不泄露“用户名是否存在”的细节。

威胁：Cookie 被前端脚本读取。

对策：

- 登录态 Cookie 设置 HttpOnly。
- 线上 HTTPS 设置 Secure。
- 根据部署域名配置 SameSite。
- 登出时后端清除 Cookie。

威胁：用户越权访问其他用户数据。

对策：

- 所有业务查询必须带当前 user_id 约束。
- 所有修改和删除操作必须校验资源归属。
- 用户隔离必须有自动化测试覆盖。

威胁：恶意文件上传。

对策：

- 限制允许的图片 MIME 类型。
- 限制文件大小。
- 生成服务端文件名，不信任用户上传的原始文件名。
- 上传文件存储在受控目录。
- 文件访问接口必须校验当前用户是否拥有该文件。

威胁：敏感信息进入日志。

对策：

- 日志不得记录密码、Cookie、数据库密码。
- 文件上传错误日志不记录完整本地敏感路径。

### 7.3 可用性

- 批量上传后必须提示待完善衣物数量。
- 衣物列表必须能筛选“待完善”。
- 标签、品类、颜色和季节操作应尽量减少重复输入，支持多选后的批量设置。
- 搭配编辑器必须允许空槽位，不强制用户选满上装、下装、鞋子、帽子。
- 搭配编辑器选择候选衣物时必须能用颜色、季节和标签筛选，减少在大量衣物中逐个切换的成本。
- 保存完全为空的搭配时必须提示错误。
- 关键删除操作需要确认。

### 7.4 可观测性

- 后端应记录关键业务事件的非敏感日志，例如注册成功、登录成功、上传成功、搭配保存成功。
- 后端应记录错误日志，便于定位文件上传、数据库连接和权限校验问题。
- 日志不得包含密码、Cookie、真实生产凭据。
- README 应说明常见运行问题的排查方式，例如数据库连接失败、上传目录不可写、端口冲突。

## 8. 分发、部署、测试与验收标准

### 8.1 分发

主要分发方式采用 Docker Compose。

要求：

- 一套 `docker compose up` 启动前端、后端和 MySQL。
- MySQL 数据使用 volume 持久化。
- 上传文件目录使用 volume 持久化。
- README 写清首次启动、停止、重建、数据目录、已知限制。
- README 写清配置方式，并强调不要提交真实生产凭据。

### 8.2 线上部署

最终需要提供可访问的 WebUI。

当前候选平台：

- Render。
- Railway。
- Fly.io。
- 其他支持容器部署的平台。

具体平台在部署阶段再次确认。当前 SPEC 不绑定某一家云平台。选择时重点考虑免费额度、MySQL 支持、volume 支持、HTTPS、部署复杂度和课程截止时间。

### 8.3 测试策略

采用前后端均衡测试策略。

后端测试：

- 认证与会话。
- 用户隔离。
- 衣物管理。
- 品类、颜色、季节和标签批量操作。
- 文件上传校验。
- 搭配保存、编辑、删除、筛选。
- 数据库迁移与 Repository 映射使用 Testcontainers MySQL，不使用 H2 替代 MySQL 行为。
- Spring Boot 测试启动时应自动执行 Flyway 迁移。

前端测试：

- 衣物列表。
- 批量上传反馈。
- 待完善筛选。
- 批量品类、颜色、季节和标签操作。
- 搭配编辑器槽位切换。
- 搭配编辑器候选衣物筛选。
- 配饰添加、拖动、层级和尺寸档位。
- 搭配方案筛选。

E2E 测试：

- 注册登录 -> 批量上传衣物 -> 批量补全品类/颜色/季节/标签 -> 创建搭配 -> 保存方案 -> 按标签筛选。
- 用户 A 不能访问用户 B 的衣物或搭配方案。

CI：

- `.gitlab-ci.yml` 必须包含名为 `unit-test` 的 job。
- `unit-test` job 至少运行后端核心测试。
- CI 需要支持 Docker 服务以运行 Testcontainers MySQL；若课程 CI 环境无法提供 Docker，必须在 README 记录限制并提供本地验证命令。
- CI 应能构建或校验前端。
- 若时间允许，CI 增加 Docker 镜像构建检查。

### 8.4 总体验收标准

项目完成时必须满足：

- 用户可以注册、登录、退出、修改昵称、修改密码。
- 用户可以批量上传衣物图片。
- 用户可以看到待完善衣物提醒，并筛选待完善衣物。
- 用户可以设置固定品类或自定义品类。
- 用户可以设置并修改衣物的品类、颜色、季节和标签。
- 用户可以批量设置品类、颜色和季节。
- 用户可以批量添加或移除标签。
- 用户可以通过搭配编辑器选择固定主槽位衣物，并按颜色、季节和标签筛选候选衣物。
- 用户可以添加自定义品类配饰，并调整位置、层级和尺寸档位。
- 用户可以保存、查看、编辑内容、编辑信息、删除搭配方案。
- 用户可以按场景标签和季节筛选搭配方案。
- 用户只能访问自己的衣物和搭配方案。
- 上传文件有类型和大小限制。
- Docker Compose 可以启动完整系统。
- GitLab CI 中 `unit-test` job 通过。
- README 包含安装、运行、分发、目录结构、安全边界说明。
- 最终提供可访问的 WebUI。

## 9. 风险与未决问题

### 9.1 风险

风险：搭配编辑器复杂度超出预期。

应对：

- 固定主槽位采用结构化选择，不做完整自由画布。
- 配饰层只支持拖动、层级和小/中/大尺寸档位。
- 不实现旋转、自由缩放、裁剪和复杂拼贴。

风险：品类、标签、季节模型混乱。

应对：

- 品类只回答“这是什么大类物品”。
- 颜色回答“主要是什么颜色”。
- 季节回答“什么时候适合穿”。
- 标签回答“还有什么个性化特征”。
- MVP 不实现子分类系统，子分类先用标签表达。

风险：subagent 把自定义品类误做成固定搭配槽位。

应对：

- SPEC 明确只有上装、下装、鞋子、帽子是固定主槽位。
- 自定义品类默认进入配饰层。
- 后续如需“自定义品类可设为固定槽位”，另做扩展，不进入 MVP。

风险：subagent 忽略用户隔离。

应对：

- PLAN 中每个涉及数据访问的 task 都必须写明 user_id 约束。
- 测试必须覆盖用户 A 不能访问用户 B 数据。

风险：文件上传实现不安全。

应对：

- PLAN 中单独拆分文件上传安全 task。
- 测试覆盖 MIME 类型、大小限制、服务端文件名和文件访问权限。

风险：Docker Compose 本地可运行，但线上部署不支持同样的 volume 或 MySQL 形态。

应对：

- 本地分发先以 Docker Compose 为准。
- 线上部署平台在部署阶段重新确认。
- README 记录线上部署与本地 Docker Compose 的差异。

风险：范围过大导致课程截止前无法完成。

应对：

- 保持“不做 AI、不做社交、不做背景去除、不做子分类系统”的边界。
- 配饰层只做最小可用交互。
- 实现计划中将核心闭环和增强体验分层安排，优先保证完整闭环。

### 9.2 未决问题

- 线上部署最终选择 Render、Railway、Fly.io 还是其他平台，需要在部署阶段再次确认。
- 线上前后端若跨站部署，`SameSite=None; Secure` 是否必要需要在部署阶段结合实际域名确认。
- 前端 Open Design 的具体设计系统与 skill 需要在进入 UI 实现计划前确认并记录。
